import { styled } from '@mui/material';
import Autocomplete from './Autocomplete';

const TextFieldBaseStyles = () => {
  return {};
};

export const StyledAutocomplete = styled(Autocomplete)(TextFieldBaseStyles);
