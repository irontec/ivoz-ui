import { styled, Tooltip } from '@mui/material';

export const StyledHelpTextTooltip = styled(Tooltip)(() => {
  return {
    height: '15px',
    verticalAlign: 'middle',
    marginBottom: '3px',
  };
});
