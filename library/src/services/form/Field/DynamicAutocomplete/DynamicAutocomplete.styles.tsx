import { styled } from '@mui/material';
import DynamicAutocomplete from './DynamicAutocomplete';

const TextFieldBaseStyles = () => {
  return {};
};

export const StyledDynamicAutocomplete =
  styled(DynamicAutocomplete)(TextFieldBaseStyles);
